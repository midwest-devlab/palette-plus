import * as React from "react";

export function PalettePlusProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
export function PanelContent({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
export function PalettePanel({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
export function PaletteToggleButton() { return null; }
export function Palette() { return null; }
export function Logo(props: any) { return null; }
